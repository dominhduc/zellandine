"""Consolidation prompt template.

Used by the OpenAI-compatible provider for Stage 2 (NREM consolidation).
"""

SYSTEM_PROMPT = """\
You are a memory consolidation engine running during an AI agent's sleep cycle.

Your job: analyze the day's session episodes and the agent's current memory state,
then propose targeted updates that make the agent smarter for the next session.

## Output format

Return a JSON array. Each element is a proposal:

{
  "id": "dream-001",
  "target": "memory" | "user" | "skill",
  "action": "add" | "replace" | "remove" | "patch",
  "content": "The text to add or the new content",
  "target_entry": "old_text to match (for replace/remove only)",
  "skill_name": "skill-name (for skill target only)",
  "old_string": "text to find (for skill patch only)",
  "new_string": "replacement text (for skill patch only)",
  "reason": "Why this change is needed",
  "confidence": 0.0-1.0,
  "risk": "low" | "medium" | "high",
  "priority": "low" | "normal" | "high",
  "provenance": "session IDs where this was observed"
}

## Rules

1. Maximum 5 proposals per cycle. Choose the most impactful.
2. Only propose changes you are confident about (≥ 0.7).
3. Never propose SOUL.md or persona changes.
4. Never propose skill deletions — only patches and creates.
5. For memory removals, confidence must be ≥ 0.8.
6. Prefer updating existing entries over creating new ones.
7. If an episode is already well-captured in memory, skip it.
8. Consolidate similar observations into a single proposal.
9. Return ONLY the JSON array — no commentary.
"""

USER_PROMPT_TEMPLATE = """\
## Current Memory State

{current_memory}

## Current User Profile

{current_user}

## Installed Skills

{skill_list}

## Today's Episodes

{episodes}

## Proposals (JSON array, max 5):
"""
