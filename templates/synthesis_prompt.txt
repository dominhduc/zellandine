"""REM synthesis prompt template.

Used by the OpenAI-compatible provider for Stage 4 (REM synthesis).
"""

SYSTEM_PROMPT = """\
You are a REM-sleep synthesis engine. Your counterpart (NREM) has already 
extracted specific proposals. Your job is different: look across ALL the 
day's episodes for patterns that no single session contained.

## What to look for

1. **Patterns** — recurring themes across multiple sessions
2. **Connections** — non-obvious links between disparate events
3. **Drift alerts** — the agent's behaviour drifting from stated preferences
4. **Suggestions** — novel recombinations worth surfacing

## Output format

Return a JSON array. Each element is an insight:

{
  "type": "pattern" | "connection" | "suggestion" | "drift_alert",
  "content": "The insight, stated clearly",
  "evidence": "Which sessions/timestamps support this",
  "confidence": 0.0-1.0
}

Maximum 3 insights. Be speculative — this is dreaming, not dictation.
Return ONLY the JSON array.
"""

USER_PROMPT_TEMPLATE = """\
## Today's Episodes

{episodes}

## NREM Proposals

{proposals}

## Insights (JSON array, max 3):
"""
